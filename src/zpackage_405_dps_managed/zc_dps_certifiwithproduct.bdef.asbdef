projection;
strict ( 2 );
use draft;

define behavior for ZC_DPS_CERTIFIWITHPRODUCT
{
  use create;
  use update;
  use delete;

  use association _stats { create; with draft; }

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}

define behavior for zc_dps_certifstprodct
{
  use update;
  use delete;

  use association _certifi { with draft; }
}
